version: 2
updates:
  # Python pins define dependency-closed Home Assistant support lanes and move
  # only with their product-owned Core and harness contracts.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
